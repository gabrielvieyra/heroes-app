import { FC } from 'react';

// Components
import { HeroeCardsContainer } from '../../components';

// Styles
import './styles.scss';

const Dc: FC = () => {
  return <HeroeCardsContainer title='DC Comics' publisher='DC Comics' />;
};

export default Dc;

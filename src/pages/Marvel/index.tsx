import { FC } from 'react';

// Components
import { HeroeCardsContainer } from '../../components';

// Styles
import './styles.scss';

const Marvel: FC = () => {
  return <HeroeCardsContainer title='Marvel Comics' publisher='Marvel Comics' />;
};

export default Marvel;
